---
phase: 02-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - src/lib/data-presence.ts
  - src/lib/data-presence.test.ts
  - src/lib/chart-data.ts
  - src/lib/chart-data.test.ts
  - src/components/StatsPanel.tsx
  - src/components/StatsPanel.css
  - src/components/StatsPanel.test.tsx
  - src/app/index.html
autonomous: true
requirements: [DASH-01, DASH-08, STYLE-01, STYLE-04]

must_haves:
  truths:
    - "User sees a summary stats panel with distance, duration, avg/max pace, avg/max HR, cadence, and total elevation gain"
    - "Dashboard gracefully hides panels when data channels are absent"
    - "Stats panel uses HUD-style monospace values with neon glow accents"
  artifacts:
    - path: "src/lib/data-presence.ts"
      provides: "Data channel detection utility"
      exports: ["detectChannels", "DataChannels"]
    - path: "src/lib/chart-data.ts"
      provides: "Chart data transformation functions"
      exports: ["speedToPace", "toPaceData", "toHrData", "toElevationData", "toCadenceData", "ChartPoint"]
    - path: "src/components/StatsPanel.tsx"
      provides: "HUD-style stats panel component"
      exports: ["StatsPanel"]
  key_links:
    - from: "src/lib/data-presence.ts"
      to: "src/types/activity.ts"
      via: "imports ActivityRecord type"
      pattern: "import type.*ActivityRecord"
    - from: "src/lib/chart-data.ts"
      to: "src/types/activity.ts"
      via: "imports ActivityRecord type"
      pattern: "import type.*ActivityRecord"
    - from: "src/components/StatsPanel.tsx"
      to: "src/types/activity.ts"
      via: "imports SummaryStats type"
      pattern: "import type.*SummaryStats"
---

<objective>
Install Phase 2 dependencies (recharts, maplibre-gl, react-maplibre, happy-dom), create data utility modules for channel detection and chart data transformation, and build the enhanced HUD-style StatsPanel component with behavioral tests.

Purpose: Establishes the library foundation and shared utilities that all subsequent Phase 2 plans depend on. The StatsPanel delivers the first visible dashboard panel with the dark/neon HUD aesthetic.

Output: Installed charting/map libraries, tested data utilities, and a tested StatsPanel component with HUD styling that renders summary metrics from the Zustand store.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts from Phase 1 that this plan consumes -->

From src/types/activity.ts:
```typescript
export type ActivityRecord = {
  timestamp: Date;
  heartRate: number | null;
  cadence: number | null;
  speed: number | null;
  altitude: number | null;
  distance: number | null;
  positionLat: number | null;
  positionLong: number | null;
};

export type SummaryStats = {
  totalDistance: number;
  totalDuration: number;
  totalTimerTime: number;
  avgSpeed: number | null;
  maxSpeed: number | null;
  avgHeartRate: number | null;
  maxHeartRate: number | null;
  avgCadence: number | null;
  totalAscent: number | null;
  totalDescent: number | null;
};

export type LapRecord = {
  startTime: Date;
  totalDistance: number;
  totalTimerTime: number;
  avgSpeed: number | null;
  avgHeartRate: number | null;
  avgCadence: number | null;
  maxHeartRate: number | null;
  maxSpeed: number | null;
};

export type NormalizedActivity = {
  records: ActivityRecord[];
  summary: SummaryStats;
  metadata: ActivityMetadata;
  laps: LapRecord[];
};
```

From src/store/activity-store.ts:
```typescript
export const useActivityStore = create<ActivityState>(...);
// Selector pattern: useActivityStore((s) => s.activity)
```

Existing CSS custom properties (from src/components/DropZone.css :root):
```css
--neon-primary: #00ffaa;
--neon-secondary: #00ccff;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies and create data utility modules</name>
  <files>package.json, bun.lock, src/lib/data-presence.ts, src/lib/data-presence.test.ts, src/lib/chart-data.ts, src/lib/chart-data.test.ts</files>
  <behavior>
    - detectChannels returns { hasGps: true, hasHeartRate: true, hasCadence: true, hasAltitude: true } for records with all fields present
    - detectChannels returns all false for records with all null fields
    - detectChannels returns { hasGps: false, hasHeartRate: true, ... } for records missing GPS but having HR
    - detectChannels returns all false for empty records array
    - speedToPace(3.33) returns approximately 5.0 (min/km for a 5:00/km pace)
    - speedToPace(0) returns 15 (capped max pace)
    - speedToPace(0.1) returns 15 (near-zero speeds capped)
    - toPaceData filters out records with null speed, converts speed to pace, computes elapsed minutes from startTime
    - toHrData filters out records with null heartRate, returns elapsed + value pairs
    - toElevationData filters out records with null altitude
    - toCadenceData filters out records with null cadence
  </behavior>
  <action>
    1. Run `bun add recharts react-is@^19.0.0 @vis.gl/react-maplibre maplibre-gl` to install all Phase 2 dependencies.

    2. Run `bun add -d @happy-dom/global-registrator` to install happy-dom for component testing (needed by StatsPanel.test.tsx, LapTable.test.tsx, RouteMap.test.tsx in this and later plans).

    3. Create `src/lib/data-presence.ts`:
       - Export type `DataChannels = { hasGps: boolean; hasHeartRate: boolean; hasCadence: boolean; hasAltitude: boolean }`
       - Export function `detectChannels(records: ActivityRecord[]): DataChannels` that checks if ANY record in the array has non-null values for each channel
       - For GPS, check both positionLat AND positionLong are non-null
       - Import type ActivityRecord from `../types/activity.ts`

    4. Create `src/lib/data-presence.test.ts` with tests for: all present, all absent, partial channels, empty array.

    5. Create `src/lib/chart-data.ts`:
       - Export type `ChartPoint = { elapsed: number; value: number }`
       - Export `speedToPace(speedMs: number): number` -- converts m/s to min/km, capped at 15 for speeds <= 0.2 m/s
       - Export `toPaceData(records: ActivityRecord[], startTime: number): ChartPoint[]` -- filters null speed, converts to pace, elapsed in minutes
       - Export `toHrData(records: ActivityRecord[], startTime: number): ChartPoint[]` -- filters null heartRate
       - Export `toElevationData(records: ActivityRecord[], startTime: number): ChartPoint[]` -- filters null altitude
       - Export `toCadenceData(records: ActivityRecord[], startTime: number): ChartPoint[]` -- filters null cadence
       - For all functions, elapsed = (record.timestamp.getTime() - startTime) / 1000 / 60 (minutes)

    6. Create `src/lib/chart-data.test.ts` with tests for speedToPace edge cases and each transformation function.

    TDD: Write tests first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>bun test src/lib/data-presence.test.ts src/lib/chart-data.test.ts</automated>
  </verify>
  <done>All data utility tests pass. recharts, react-is, @vis.gl/react-maplibre, maplibre-gl, and @happy-dom/global-registrator are installed in package.json.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create HUD-style StatsPanel component with behavioral tests</name>
  <files>src/components/StatsPanel.tsx, src/components/StatsPanel.css, src/components/StatsPanel.test.tsx, src/app/index.html</files>
  <behavior>
    - Renders all stat cards: Distance, Duration, Avg Pace, Max Pace, Avg HR, Max HR, Cadence, Elevation Gain
    - formatDistance(5000) renders as "5.00" (meters to km, 2 decimal places)
    - formatDuration(3661) renders as "1:01:01" (h:mm:ss for durations over 1 hour)
    - formatDuration(300) renders as "5:00" (m:ss for durations under 1 hour)
    - formatPace renders speed m/s as "M:SS" pace string (e.g., 3.33 m/s renders as "5:00")
    - Null avgHeartRate renders as "--" (not "null" or empty)
    - Null maxHeartRate renders as "--"
    - Null avgCadence renders as "--"
    - Null totalAscent renders as "--"
    - Full SummaryStats fixture renders all values with correct formatting
  </behavior>
  <action>
    1. Create `src/components/StatsPanel.test.tsx` FIRST (RED phase):
       - Use `import { test, expect, describe } from "bun:test"` and `import { renderToString } from "react-dom/server"` for SSR-based testing (avoids need for DOM environment for basic rendering checks)
       - Create a SummaryStats fixture with all fields populated: `{ totalDistance: 10000, totalDuration: 3600, totalTimerTime: 3500, avgSpeed: 3.33, maxSpeed: 4.0, avgHeartRate: 155, maxHeartRate: 180, avgCadence: 170, totalAscent: 250, totalDescent: 230 }`
       - Create a partial SummaryStats fixture with null fields: `{ ...fullFixture, avgHeartRate: null, maxHeartRate: null, avgCadence: null, totalAscent: null }`
       - Test: "renders all stat cards" -- renderToString(<StatsPanel summary={fullFixture} />) contains "10.00" (distance in km), "1:00:00" or similar (duration), stat card elements
       - Test: "formats distance from meters to km" -- output contains "10.00"
       - Test: "formats duration correctly" -- output contains expected time string
       - Test: "formats pace from speed" -- output contains the expected pace string (e.g., "5:00" for 3.33 m/s)
       - Test: "shows -- for null values" -- renderToString with partial fixture contains "--" for null HR, cadence, elevation
       - Test: "does not show null or undefined text" -- renderToString with partial fixture does NOT contain the literal string "null" or "undefined"

    2. Run tests to confirm RED (failing because StatsPanel.tsx does not exist yet).

    3. Create `src/components/StatsPanel.tsx` (GREEN phase):
       - Accept props: `{ summary: SummaryStats }` (import type from `../types/activity.ts`)
       - Export format helper functions so tests can import them if needed: formatDistance, formatDuration, formatPace, formatNullable
       - formatDistance(meters: number): string -- (meters / 1000).toFixed(2)
       - formatDuration(seconds: number): string -- if >= 3600: h:mm:ss, else m:ss (pad seconds and minutes to 2 digits)
       - formatPace(speedMs: number | null): string -- if null or <= 0.2 return "--", else convert m/s to min/km and format as M:SS
       - formatNullable(value: number | null, decimals?: number): string -- return "--" for null, else value.toFixed(decimals ?? 0)
       - Render 8 stat cards in a flex row: Distance (km), Duration (mm:ss or h:mm:ss), Avg Pace (min/km), Max Pace (min/km), Avg HR (bpm), Max HR (bpm), Cadence (spm), Elevation Gain (m)
       - Each stat card has: label (uppercase small text), value (large monospace font), unit (small dimmed text)
       - BEM naming: `.stats-panel`, `.stats-panel__card`, `.stats-panel__label`, `.stats-panel__value`, `.stats-panel__unit`

    4. Create `src/components/StatsPanel.css`:
       - `.stats-panel`: flex row, gap 0.75rem, flex-wrap: wrap
       - `.stats-panel__card`: flex: 1 1 120px, dark translucent bg (rgba(10,14,20,0.85)), border 1px solid rgba(0,255,170,0.12), border-radius 6px, padding 0.75rem 0.85rem
       - Add subtle neon border glow: box-shadow: 0 0 8px rgba(0,255,170,0.06), inset 0 1px 0 rgba(255,255,255,0.03)
       - `.stats-panel__label`: font-size 0.65rem, uppercase, letter-spacing 0.08em, color rgba(224,224,224,0.45)
       - `.stats-panel__value`: font-size 1.3rem, font-family 'JetBrains Mono', 'Fira Code', 'SF Mono', 'Consolas', monospace, font-weight 600, color var(--neon-primary, #00ffaa), text-shadow 0 0 10px rgba(0,255,170,0.3)
       - `.stats-panel__unit`: font-size 0.65rem, color rgba(224,224,224,0.3), margin-left 0.25rem
       - Add a subtle scan-line pseudo-element on the card (repeating-linear-gradient with 1px transparent, 1px rgba(255,255,255,0.01)) for HUD texture

    5. Add MapLibre GL CSS to `src/app/index.html`:
       - Add `<link rel="stylesheet" href="https://unpkg.com/maplibre-gl@5.20.0/dist/maplibre-gl.css" />` in the `<head>` section (per RESEARCH.md recommendation to use CDN link as safe approach)

    6. Run tests to confirm GREEN. Also verify TypeScript compiles: `bunx tsc --noEmit`
  </action>
  <verify>
    <automated>bun test src/components/StatsPanel.test.tsx</automated>
  </verify>
  <done>StatsPanel component renders 8 stat cards with HUD monospace styling and neon glow. All behavioral tests pass: correct formatting for distance/duration/pace, "--" for null values. MapLibre CSS linked in index.html. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `bun test` passes all new and existing tests
- `bunx tsc --noEmit` compiles without errors
- recharts, react-is, @vis.gl/react-maplibre, maplibre-gl in package.json dependencies
- @happy-dom/global-registrator in devDependencies
- src/lib/data-presence.ts exports detectChannels and DataChannels
- src/lib/chart-data.ts exports speedToPace, toPaceData, toHrData, toElevationData, toCadenceData, ChartPoint
- src/components/StatsPanel.tsx exports StatsPanel
- src/components/StatsPanel.test.tsx tests pass with behavioral coverage
- src/app/index.html contains maplibre-gl CSS link
</verification>

<success_criteria>
- All Phase 2 libraries installed and importable
- Data presence detection tested for all channel combinations
- Chart data transformations tested for edge cases (null filtering, pace capping, elapsed computation)
- StatsPanel renders with HUD aesthetic (monospace, neon glow, dark cards)
- StatsPanel behavioral tests verify: card count, distance/duration/pace formatting, "--" for nulls
- TypeScript compiles cleanly across all files
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard/02-01-SUMMARY.md`
</output>
