---
phase: 02-dashboard
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/RouteMap.tsx
  - src/components/RouteMap.css
  - src/components/RouteMap.test.tsx
  - src/components/LapTable.tsx
  - src/components/LapTable.css
  - src/components/LapTable.test.tsx
autonomous: true
requirements: [DASH-06, DASH-07]

must_haves:
  truths:
    - "User sees a GPS route map rendered on a dark basemap when GPS data is present"
    - "User sees a lap/split table with per-split metrics"
    - "Route map auto-zooms to fit the GPS track with padding"
  artifacts:
    - path: "src/components/RouteMap.tsx"
      provides: "MapLibre GL route map component"
      exports: ["RouteMap", "recordsToCoords", "computeBounds"]
    - path: "src/components/LapTable.tsx"
      provides: "Lap splits table component"
      exports: ["LapTable"]
  key_links:
    - from: "src/components/RouteMap.tsx"
      to: "@vis.gl/react-maplibre"
      via: "Map, Source, Layer components"
      pattern: "import.*from.*react-maplibre"
    - from: "src/components/RouteMap.tsx"
      to: "src/types/activity.ts"
      via: "imports ActivityRecord for GPS coordinate extraction"
      pattern: "import type.*ActivityRecord"
    - from: "src/components/LapTable.tsx"
      to: "src/types/activity.ts"
      via: "imports LapRecord for table rendering"
      pattern: "import type.*LapRecord"
---

<objective>
Build the GPS route map component using MapLibre GL JS with Carto Dark Matter basemap, and the lap splits table with per-split metrics and neon HUD styling. Both components include behavioral tests for their pure logic.

Purpose: Delivers the spatial visualization (route map) and tabular data (lap splits) panels. These are the two bottom-row panels in the dashboard layout.

Output: RouteMap component rendering a GeoJSON LineString on a dark basemap (with tested pure functions), and LapTable component rendering per-lap metrics in a styled table (with tested rendering behavior).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard/02-RESEARCH.md
@.planning/phases/02-dashboard/02-01-SUMMARY.md

<interfaces>
<!-- From Phase 1 types -->

From src/types/activity.ts:
```typescript
export type ActivityRecord = {
  timestamp: Date;
  heartRate: number | null;
  cadence: number | null;
  speed: number | null;
  altitude: number | null;
  distance: number | null;
  positionLat: number | null;
  positionLong: number | null;
};

export type LapRecord = {
  startTime: Date;
  totalDistance: number;
  totalTimerTime: number;
  avgSpeed: number | null;
  avgHeartRate: number | null;
  avgCadence: number | null;
  maxHeartRate: number | null;
  maxSpeed: number | null;
};
```

MapLibre + react-maplibre (installed in Plan 01):
```typescript
import { Map, Source, Layer } from "@vis.gl/react-maplibre";
// CSS loaded via CDN link in index.html (Plan 01)
```

Carto Dark Matter style URL:
```
https://basemaps.cartocdn.com/gl/dark-matter-gl-style/style.json
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RouteMap component with MapLibre GL and tested pure functions</name>
  <files>src/components/RouteMap.tsx, src/components/RouteMap.css, src/components/RouteMap.test.tsx</files>
  <behavior>
    - recordsToCoords: filters records with non-null positionLat AND positionLong, returns [lng, lat] tuples (GeoJSON order)
    - recordsToCoords: records with null GPS fields are excluded
    - recordsToCoords: empty array returns empty array
    - recordsToCoords: coordinate order is [longitude, latitude] (NOT [lat, lng])
    - computeBounds: given coords [[10,20],[30,40]], returns { minLng: 10, minLat: 20, maxLng: 30, maxLat: 40 }
    - computeBounds: single coordinate returns that point as both min and max
    - Fewer than 2 valid GPS coordinates: component returns null (no map rendered)
  </behavior>
  <action>
    1. Create `src/components/RouteMap.test.tsx` FIRST (RED phase):
       - Import { test, expect, describe } from "bun:test"
       - Tests for `recordsToCoords` (exported pure function):
         - Test: records with all GPS fields present returns [lng, lat] tuples
         - Test: records with null positionLat or positionLong are excluded
         - Test: empty records array returns empty array
         - Test: coordinate order is [positionLong, positionLat] -- verify first element of tuple matches positionLong, second matches positionLat
       - Tests for `computeBounds` (exported pure function):
         - Test: two coordinates computes correct min/max for lng and lat
         - Test: single coordinate returns that point as both min and max
         - Test: handles negative coordinates correctly (e.g., Western hemisphere longitudes)
       - Test: fewer than 2 valid GPS coordinates -- this can be tested by checking recordsToCoords output length, confirming the "return null" guard condition
       - NO WebGL or DOM required -- these are pure function tests

    2. Run tests to confirm RED (failing because RouteMap.tsx does not exist yet).

    3. Create `src/components/RouteMap.tsx` (GREEN phase):
       - Export pure function `recordsToCoords(records: ActivityRecord[]): [number, number][]` -- filter records where BOTH positionLat AND positionLong are non-null, map to [positionLong, positionLat] tuples (GeoJSON is [lng, lat] order -- see RESEARCH.md Pitfall 6)
       - Export pure function `computeBounds(coords: [number, number][]): { minLng: number; minLat: number; maxLng: number; maxLat: number }` -- find min/max lng and lat from coordinate array
       - Main component accepts props: `{ records: ActivityRecord[] }` (import type from `../types/activity.ts`)
       - Import `{ Map, Source, Layer }` from `@vis.gl/react-maplibre`
       - Call recordsToCoords(records) to extract coordinates
       - If fewer than 2 valid coordinates, return null (nothing to render)
       - Call computeBounds(coords) for auto-zoom
       - Build GeoJSON Feature with LineString geometry from coordinates
       - Render `<Map>` with:
         - `initialViewState={{ bounds: [[bounds.minLng, bounds.minLat], [bounds.maxLng, bounds.maxLat]], fitBoundsOptions: { padding: 30 } }}`
         - `mapStyle="https://basemaps.cartocdn.com/gl/dark-matter-gl-style/style.json"`
         - `style={{ width: "100%", height: "100%" }}`
         - `attributionControl={false}` (clean HUD look)
       - Inside Map: `<Source id="route" type="geojson" data={geojson}>` with `<Layer id="route-line" type="line" paint={{ "line-color": "#00ffaa", "line-width": 2.5, "line-opacity": 0.85 }} />`
       - Wrap Map in `<div className="route-map">`

    4. Create `src/components/RouteMap.css`:
       - `.route-map`: height 100%, width 100%, border-radius 8px, overflow hidden, background rgba(10,14,20,0.6), border 1px solid rgba(255,255,255,0.06)
       - Override MapLibre's canvas border-radius to match container: `.route-map .maplibregl-canvas { border-radius: 8px; }`

    5. Run tests to confirm GREEN. Also verify: `bunx tsc --noEmit`
  </action>
  <verify>
    <automated>bun test src/components/RouteMap.test.tsx</automated>
  </verify>
  <done>RouteMap component renders a neon green GPS track on Carto Dark Matter basemap with auto-zoom bounding box. Returns null when GPS data is absent. Pure functions (recordsToCoords, computeBounds) pass all behavioral tests: correct coordinate order, bounding box computation, null filtering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create LapTable component with neon styling and behavioral tests</name>
  <files>src/components/LapTable.tsx, src/components/LapTable.css, src/components/LapTable.test.tsx</files>
  <behavior>
    - Renders correct number of table rows matching laps array length
    - Formats distance from meters to km (e.g., 1000m renders as "1.00")
    - Formats duration from seconds to M:SS (e.g., 300s renders as "5:00")
    - Formats pace from speed m/s to M:SS/km (e.g., 3.33 m/s renders as "5:00")
    - Null avgHeartRate renders as "--"
    - Null avgCadence renders as "--"
    - Null avgSpeed (pace) renders as "--"
    - Empty laps array renders table with header but no body rows
  </behavior>
  <action>
    1. Create `src/components/LapTable.test.tsx` FIRST (RED phase):
       - Import { test, expect, describe } from "bun:test" and { renderToString } from "react-dom/server"
       - Create a LapRecord[] fixture with 3 laps:
         - Lap 1: { startTime: new Date("2024-01-01T00:00:00Z"), totalDistance: 1000, totalTimerTime: 300, avgSpeed: 3.33, avgHeartRate: 155, avgCadence: 170, maxHeartRate: 165, maxSpeed: 4.0 }
         - Lap 2: { startTime: new Date("2024-01-01T00:05:00Z"), totalDistance: 1000, totalTimerTime: 310, avgSpeed: 3.23, avgHeartRate: 160, avgCadence: 168, maxHeartRate: 172, maxSpeed: 3.8 }
         - Lap 3: { startTime: new Date("2024-01-01T00:10:00Z"), totalDistance: 1000, totalTimerTime: 290, avgSpeed: null, avgHeartRate: null, avgCadence: null, maxHeartRate: null, maxSpeed: null }
       - Test: "renders correct number of rows" -- renderToString with 3 laps, count occurrences of lap-table__row or `<tr` in tbody (should be 3)
       - Test: "formats distance as km" -- output contains "1.00" for 1000m
       - Test: "formats duration as M:SS" -- output contains "5:00" for 300s
       - Test: "formats pace from speed" -- output contains the expected pace string for 3.33 m/s
       - Test: "shows -- for null HR and cadence" -- Lap 3 row contains "--" for null avgHeartRate and avgCadence
       - Test: "shows -- for null pace" -- Lap 3 with null avgSpeed shows "--" for pace
       - Test: "renders empty table for no laps" -- renderToString with empty array still renders table header, but no body rows

    2. Run tests to confirm RED (failing because LapTable.tsx does not exist yet).

    3. Create `src/components/LapTable.tsx` (GREEN phase):
       - Accept props: `{ laps: LapRecord[] }` (import type from `../types/activity.ts`)
       - Render an HTML table with columns: Split (#), Distance (km), Time (m:ss), Pace (min:sec/km), Avg HR (bpm), Cadence (spm)
       - Format helpers (defined locally or exported for testing):
         - formatPace: speed m/s to "M:SS" min/km, return "--" for null/zero
         - formatDuration: seconds to "M:SS"
         - formatDistance: meters to km with 2 decimal places
       - Map over laps with index, display lap number (i + 1)
       - For null values (avgHeartRate, avgCadence), display "--"
       - BEM naming: `.lap-table`, `.lap-table__table`, `.lap-table__header`, `.lap-table__row`, `.lap-table__cell`
       - Wrap in `<div className="lap-table">`

    4. Create `src/components/LapTable.css`:
       - `.lap-table`: height 100%, overflow-y auto, background rgba(10,14,20,0.6), border 1px solid rgba(255,255,255,0.06), border-radius 8px, padding 0.5rem
       - `.lap-table__table`: width 100%, border-collapse collapse, font-size 0.8rem
       - `th` (header cells): text-transform uppercase, font-size 0.65rem, letter-spacing 0.06em, color rgba(224,224,224,0.4), padding 0.5rem 0.6rem, text-align left, border-bottom 1px solid rgba(0,255,170,0.1)
       - `td` (data cells): padding 0.4rem 0.6rem, color rgba(224,224,224,0.75), border-bottom 1px solid rgba(255,255,255,0.04), font-family monospace (same stack as StatsPanel)
       - `tr:hover td`: background rgba(0,255,170,0.03) for subtle row highlight
       - Pace column values in neon-primary color for emphasis

    5. Run tests to confirm GREEN. Also verify: `bunx tsc --noEmit`
  </action>
  <verify>
    <automated>bun test src/components/LapTable.test.tsx</automated>
  </verify>
  <done>LapTable component renders per-split metrics with monospace values, neon-accented borders, and hover row highlights. All behavioral tests pass: correct row count, distance/duration/pace formatting, "--" for null HR/cadence/pace, empty laps renders header only.</done>
</task>

</tasks>

<verification>
- `bun test src/components/RouteMap.test.tsx` passes all pure function tests
- `bun test src/components/LapTable.test.tsx` passes all rendering tests
- `bunx tsc --noEmit` passes with RouteMap and LapTable
- `bun test` passes (no regressions)
- RouteMap uses [lng, lat] coordinate order for GeoJSON
- RouteMap returns null when < 2 GPS coordinates
- LapTable handles null avgHeartRate, avgCadence, and avgSpeed gracefully
- Both components follow BEM CSS naming convention
</verification>

<success_criteria>
- RouteMap renders a neon green track on a dark basemap with auto-zoom
- RouteMap pure functions tested: recordsToCoords filters nulls and uses [lng,lat] order, computeBounds calculates correct min/max
- LapTable renders formatted lap metrics in a styled table
- LapTable tested: correct row count, formatting, "--" for nulls
- Both components have dark translucent backgrounds consistent with the HUD theme
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard/02-03-SUMMARY.md`
</output>
